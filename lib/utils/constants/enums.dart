// list of enums, and they can not be instantiated in a class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, pending, served }

enum PaymentMethod { tigoesa, airtelmoney, mpesa, halopesa, tpesa }
